# Managed brand assets

These production SVGs are copied verbatim from the sibling
`lfpconnect-assets/assets/svg` catalog. Update them from that catalog rather
than drawing independent variants in this application.
